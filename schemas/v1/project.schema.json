{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ppt-ops.local/schemas/v1/project.schema.json",
  "title": "V1 Project",
  "allOf": [
    { "$ref": "entity.schema.json" },
    {
      "type": "object",
      "required": ["title", "format", "outputs", "source_ids", "outline_id", "theme_id", "asset_ids"],
      "properties": {
        "kind": { "const": "project" },
        "format": { "const": "16:9" },
        "delivery_mode": { "type": "string", "enum": ["live_talk", "workshop", "pitch", "leave_behind", "async"] },
        "accessibility_profile": {
          "type": "object",
          "required": ["enabled", "intent", "document_language", "reading_direction", "target_formats"],
          "properties": {
            "enabled": { "const": true },
            "intent": { "type": "string", "enum": ["create_accessible", "audit_only", "remediate"] },
            "document_language": { "type": "string", "minLength": 1 },
            "reading_direction": { "type": "string", "enum": ["ltr", "rtl"] },
            "target_formats": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "type": "string", "enum": ["html", "pptx", "pdf", "png"] } },
            "text_scale": { "type": "number", "minimum": 1 },
            "standard_target": { "type": "object", "required": ["name", "version"], "properties": { "name": { "type": "string", "minLength": 1 }, "version": { "type": "string", "minLength": 1 } }, "additionalProperties": false },
            "audience_needs": { "$ref": "#/$defs/textList" },
            "organization_policies": { "$ref": "#/$defs/textList" },
            "required_evidence": { "type": "array", "uniqueItems": true, "items": { "type": "string", "enum": ["human_accessibility_review", "assistive_technology", "powerpoint_accessibility_checker", "organization_approval"] } },
            "assumptions": { "$ref": "#/$defs/textList" },
            "exceptions": { "$ref": "#/$defs/textList" },
            "unresolved_risks": { "$ref": "#/$defs/textList" }
          },
          "additionalProperties": false
        }
      }
    }
  ],
  "$defs": { "textList": { "type": "array", "items": { "type": "string", "minLength": 1 } } }
}
