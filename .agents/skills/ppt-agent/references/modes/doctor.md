# Doctor Mode

Inspect Skill/Core compatibility, the System/User manifest, dependencies, fonts, optional renderers, and the named project's portable truth. Rebuild only derived indexes when requested. Missing optional software is degraded or pending, never a false pass. Exit with machine-readable actionable diagnostics.
