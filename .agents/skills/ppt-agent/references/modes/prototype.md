# Prototype Mode

Build a representative small page set when visual direction is uncertain. Apply `visual-quality.md`, include different content shapes and rhythm, validate structure, render evidence when available, and request focused feedback. Exit with an accepted direction or explicit changes; never regenerate unselected approved pages.
