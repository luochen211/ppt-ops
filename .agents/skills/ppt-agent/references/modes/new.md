# New Mode

Resolve the Data Root, establish a safe relative project name, and create the project without overwriting a non-empty directory. Capture audience, purpose, language, approximate length, delivery context, style, and factual boundaries. Exit with a valid project and brief, then reroute to `intake` when files exist or `outline` when the brief is the approved source.
