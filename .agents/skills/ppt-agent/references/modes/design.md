# Design Mode

Use the accepted Outline, profile brand rules, semantic template catalog, `visual-quality.md`, and `visual-assets.md`. Define Theme tokens, opening job, page rhythm, page-template choices, asset needs, and density budgets without changing facts. For each generated asset need, define its semantic action and role before choosing a style preset. Exit with an accepted design direction. Load Source bodies only when a cited visual decision requires authorization.
