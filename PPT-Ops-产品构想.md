# PPT-Ops：稳定产出 PPT 的 Agent 工作台

> 状态：产品构想 v0.1
>
> 本文记录目前关于“如何让 Agent 稳定产出可交付 PPT”的共同思路。它不是单个 PPT Skill 的说明，也不是某一套视觉模板的使用手册。

## 参考材料

本目录同时保留两份完整参考资料：

- [顶峰开班直播 PPT 完整归档](<references/顶峰开班直播-PPT完整归档-2026-09-01/>)：真实项目的对话、逐字稿、PPTX、PDF、图片、源码、脚本、JSONL 会话和验收资料。它用于研究内容真源、页面认知任务、版本控制、人工确认和交付验证。
- [guizang-ppt-skill](<references/guizang-ppt-skill/>)：完整 Skill 源文件，包含 `SKILL.md`、模板、布局、主题、配图规范、截图处理说明和验证脚本。它用于研究视觉版式、节奏、图片槽位和视觉自检。

两份资料均为复制件，原始文件未被移动、删除或改写。

## 一、产品定位

PPT-Ops 是一套面向复杂演讲、课程、商业发布和项目交付的 PPT Agent 工作台。

它的目标不是让 Agent 偶尔生成一套漂亮的 PPT，而是让 PPT 生产过程具备：

- 可重复：相似任务可以沿着相似流程完成；
- 可控制：每个阶段都有明确边界，不能任意跳步；
- 可验证：内容、结构、视觉和文件交付分别有证据；
- 可迭代：用户反馈可以沉淀为项目规则和系统能力；
- 可交付：最终结果不仅能看，还能编辑、播放、分享和追溯。

核心判断：

> `Skill` 是能力模块；PPT-Ops 是让这些能力稳定运行的生产系统。

## 二、与 Skill 的关系

PPT-Ops 不替代 Skill，而是管理和调用 Skill。

```text
PPT-Ops 工作台
├── 需求路由与任务状态
├── 项目规则与数据契约
├── 内容真源与版本管理
├── 页面任务与叙事规划
├── 生产、审查和交付工作流
└── Skill / 工具适配层
    ├── guizang-ppt-skill：视觉节奏、版式和审美检查
    ├── pptx：原生 PPTX 生成、读取和修改
    ├── imagegen：配图或视觉素材生成
    └── render / validate：渲染、结构、字体和几何检查
```

### `guizang-ppt-skill` 负责什么

- 页面视觉节奏；
- Hero、章节幕封和收束页的层级；
- 图片比例、图片槽位和图片网格；
- 字体、颜色、留白和版式一致性；
- 浏览顺序和演讲观看体验；
- 视觉层面的自检清单。

它不应该决定项目的业务事实、叙事目标、页面数量或最终交付格式，也不应强行把原生 PPTX 项目改造成 HTML 网页 PPT。

### PPT-Ops 负责什么

- 先判断用户当前处于诊断、规划、设计、制作、审查还是交付阶段；
- 判断哪些文件是内容真源，哪些只是参考资料；
- 控制什么时候可以生成候选稿、什么时候可以写入当前版本；
- 管理用户确认、版本、素材、失败记录和验收证据；
- 把多个 Skill 组织成一条可重复执行的生产链。

## 三、借鉴 career-ops 的核心思想

PPT-Ops 应借鉴 `career-ops` 的系统形态，而不是只复制它的文件名。

### 1. 用路由表进入明确模式

不同请求进入不同工作模式，不能让 Agent 每次从零自由发挥。

建议的第一组模式：

| 模式 | 任务 |
|---|---|
| `intake` | 收集主题、受众、场景、时长、素材和交付约束 |
| `diagnose` | 分析逐字稿、旧 PPT 和观众认知任务 |
| `outline` | 生成叙事弧、章节和逐页任务 |
| `design` | 确定页面信息关系、视觉语法和图片任务 |
| `prototype` | 生成关键页候选稿，等待用户确认方向 |
| `build` | 生成或修改原生 PPTX |
| `review` | 做内容、结构、视觉和播放环境检查 |
| `handoff` | 输出 PPTX、PDF、预览图、素材清单和修改记录 |
| `archive` | 保存对话、源文件、版本、脚本和验收证据 |

### 2. 用数据契约保护项目资料

项目必须区分系统层、项目层和产物层：

```text
系统层：通用规则、模式、Skill 适配、验证脚本
项目层：逐字稿、品牌规则、Project Brief、PPT 逻辑、素材清单
产物层：候选稿、当前版本、最终 PPTX、PDF、预览图、验收报告
```

系统更新不能覆盖项目内容；生成新版本不能覆盖旧版本；历史素材不能反向覆盖用户最新确认的结果。

### 3. 保持内容真源边界

页面文字、数据、案例、品牌承诺和事实必须来自：

- 用户当前明确提供的内容；
- 项目指定的逐字稿和内容真源；
- 用户确认过的历史页面或素材。

Skill、旧模板和模型常识只能帮助表达，不能自行增加未经确认的业务事实。

### 4. 把人工确认放在关键节点

Agent 可以分析和生成，但以下节点应保留用户确认：

- 大纲和页面分工确认；
- 关键页视觉方向确认；
- 是否开启新版本；
- 是否把候选稿合并进当前版本；
- 最终交付确认。

“继续”“可以”只表示继续当前工作，不自动等于版本冻结或正式交付。

## 四、核心生产流程

```text
需求进入
  ↓
识别任务类型、受众、演讲场景和交付格式
  ↓
读取内容真源、项目规则、当前版本和必要素材
  ↓
生成叙事结构与逐页认知任务
  ↓
为每页定义“三秒内必须看懂什么”
  ↓
确定信息关系：并列、因果、层级、流程、循环或对比
  ↓
选择视觉语法、布局和图片承担的任务
  ↓
制作一张关键页或小范围候选稿
  ↓
用户确认方向
  ↓
生成或修改原生 PPTX
  ↓
内容、结构、渲染、字体和几何检查
  ↓
实际 PowerPoint 播放环境检查
  ↓
用户验收
  ↓
交付与完整归档
```

核心原则：

> 先判断页面为什么存在，再决定页面长什么样；先验证候选稿，再批量生产。

## 五、页面设计的最小单位

每一页在开始制作前必须记录以下字段：

```yaml
page: 23
source: 逐字稿第 243-260 行
task: 让观众理解“症状不等于病因”
three_second_message: 看见表面症状只是猜测，进入诊断才能定位问题
relation: before_after
screen_text: 用户确认后的上屏文字
visual_job: 画面承担模糊判断与精准诊断的对照
asset_slots:
  - type: generated_visual
    ratio: 16:9
    text_in_image: false
status: prototype
```

这样可以避免“有内容但不知道这一页要完成什么”，也避免先套模板再把文字硬塞进去。

## 六、稳定产出的质量门

稳定不是每一页长得一样，而是每一页都经过同一套必要判断。

### 内容门

- 页面任务与逐字稿顺序一致；
- 没有新增未经确认的事实、案例、数据或承诺；
- 独立条目、编号和限定条件没有被错误合并；
- 页面文字和口播承担的职责清楚。

### 关系门

- 线、节点、容器和箭头都有明确语义；
- 并列、从属、因果、循环和触发没有被视觉误读；
- 去掉装饰性小字后，主关系仍然成立；
- 每页只有一个主要观看入口。

### 视觉门

- 标题层级、正文层级和元信息角色清楚；
- 图片比例、裁切和留白适合实际槽位；
- 没有连续多页重复同一种结构；
- 图片是证据、关系或记忆点，不是填空装饰；
- `guizang-ppt-skill` 的视觉检查项通过。

### 文件门

- PPTX 可以打开、编辑和播放；
- 字体在实际播放设备中存在；
- 没有文字溢出、对象重叠、负几何或异常裁切；
- PDF、逐页预览和原生 PPTX 的页序一致；
- 所有源文件、素材、脚本和版本关系可以追溯。

## 七、图片与素材原则

图片不是统一的装饰层，而是页面任务的一部分。

图片可以承担：

- 真实证据；
- 场景记忆；
- 抽象关系的视觉化；
- 症状和病因的对照；
- 产品、流程或界面的真实展示。

图片不能承担：

- 伪造成绩、证件、聊天记录、学生案例或业务数据；
- 把必须精确阅读的中文正文塞进生成图；
- 用氛围图掩盖页面关系没有设计清楚的问题。

默认策略是：图片负责画面和关系，中文标题、标签、数据和关键说明使用 PPT 原生文字叠加。

## 八、第一版 MVP

第一版不需要立刻覆盖所有类型的 PPT，先完成一条稳定闭环：

```text
用户提供逐字稿 + 旧 PPT + 素材
  ↓
Agent 输出逐页任务和大纲
  ↓
用户确认关键页方向
  ↓
生成原生可编辑 PPTX
  ↓
输出 PDF、逐页 PNG 和验收报告
  ↓
用户反馈进入下一轮修改
```

建议第一版只支持：

- 16:9 原生 PPTX；
- 单一项目目录；
- 手动触发模式；
- 关键页候选稿；
- 页面级修改；
- 基础文字、结构和渲染验证；
- 完整对话与文件归档。

暂不优先做：

- 全自动批量生成所有类型 PPT；
- 自动替用户发布或分享；
- 强制统一所有项目的视觉风格；
- 直接把网页 PPT 转成 PPTX；
- 没有用户确认的整套重构。

## 九、产品判断

目前最重要的产品决策是：

> PPT-Ops 的核心产品不是“生成器”，而是“可控的 PPT 生产流程”。

它应当把以下四类能力连接起来：

```text
内容理解
  + 页面设计
  + 文件生产
  + 证据验收
```

`career-ops` 提供的是 Agent 工作台和流程控制的参考；
当前顶峰 PPT 归档提供的是内容真源、页面认知任务、版本边界和实际交付经验；
`guizang-ppt-skill` 提供的是视觉节奏、版式审查和视觉生产经验。

三者组合后，才有机会形成一个真正稳定的 PPT 生产产品。

## 十、下一步待决定的问题

1. 第一版只做原生 PPTX，还是同时支持 HTML 网页 PPT？
2. 目标用户是个人创作者、教育团队、设计师，还是乙方交付团队？
3. 第一版优先解决“从零生成”，还是“基于现有 PPT 稳定修改”？
4. 页面状态是否需要独立的 `page-spec.yaml` 或 JSON 文件？
5. 哪些检查必须自动化，哪些保留人工审查？
6. 反馈沉淀为项目规则，还是进入通用 Skill / 系统层？
